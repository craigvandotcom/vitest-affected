# Constraints: coupling-channel taxonomy + BCA replay-corpus bounds

Sources: `src/plugin.ts`, `src/graph/builder.ts`, `src/graph/normalize.ts`, `src/changed-files.ts`; BCA at `/Users/craigvanheerden/Repos/neometa/software/body-compass-app`.

## Part A — Coupling-channel taxonomy

| Channel | Verdict | vitest-affected evidence | BCA usage evidence | Severity |
|---|---|---|---|---|
| vi.mock/vi.doMock, factory replaces module | BY-DESIGN (not a hole) | No mock-aware code anywhere in src/; oxc-parser (`builder.ts`) never sees call expressions, only `staticImports`/`dynamicImports`/`staticExports`. Runtime edge only forms if the real file actually loads. | 341 test files mock via `@/`/relative specifiers (`grep -rln "vi\.mock(['\"](\.\|@/)"`). Confirmed case: `app/(protected)/app/ingredients/[slug]/__tests__/ingredient-page-client.test.tsx` mocks `@/lib/supabase/client` + `@/lib/utils/api-client` with a factory and has **no** static import of either — real module never loads, no edge. Correct: mock decouples test from real impl. | Low (inherent to mocking, not tool-specific) |
| globalSetup file | SILENT-HOLE | `plugin.ts` L448–466 checks only `project.config.setupFiles`; `globalSetup` never referenced in src/. Not in `CONFIG_BASENAMES` (L76–101). | `vitest.config.mts` has no `globalSetup` field — channel unexercised today. | None now / latent |
| setupFiles change | TRACKED | `plugin.ts` L448–466: explicit path-set match → forces full suite. | `setupFiles: ['__tests__/setup/vitest.setup.ts']` confirmed. | Covered |
| CSS / CSS-module imports | TRACKED (runtime only) | `builder.ts` `BINARY_EXTENSIONS` (L6–12) skips `.css/.scss/.sass/.less` in static delta-parse; runtime reporter (`plugin.ts` L121–150) has no extension filter, so a warm cache still records CSS load edges via `importDurations`. | 3 plain `.css` imports; 0 `.module.css` hits. | Low usage |
| import.meta.glob | SILENT-HOLE (new-file case) | oxc-parser doesn't recognize `import.meta.glob(...)`; not extracted by `builder.ts`. A **new file** matching an existing glob pattern isn't itself a "changed" seed and the glob-caller didn't change → never re-selected. | 0 usages (grep clean). | N/A today |
| ?raw / ?url queries | TRACKED | `normalize.ts` L12–14 strips query strings before graph lookup, so variants converge on the base file path correctly. | 0 usages. | N/A today |
| env vars / import.meta.env / Vite `define` | SILENT-HOLE | No `define`/`import.meta.env` handling anywhere in src/; `.env` files aren't modules and aren't in `CONFIG_BASENAMES`. | 74 test files read `process.env.*`; `vitest.setup.ts` hardcodes `DEMO_EMAILS` inline (so mitigated via the setupFiles channel) but an external `.env.local`/CI-secret change has zero trigger. No `define:` block present. | Real but mitigated |
| new Worker(new URL(...)) | SILENT-HOLE | `new Worker(...)` is a `NewExpression`, not import syntax — invisible to `builder.ts`'s static extraction. | `lib/utils/worker-compression.ts` uses `new Worker(` — live usage, not theoretical. | Moderate |
| fs.readFile'd fixtures | FALLBACK-COVERED (if configured) | Purpose-built `fullSuiteTriggers` option (`plugin.ts` L54–68, L372–387). | BCA wires `fullSuiteTriggers: ['__tests__/fixtures/']`; 34 files call `readFileSync`/`readFile` in tests. Not verified that all 34 live under that one path — fixtures outside it would be a silent hole. CI additionally runs `test:all` (VITEST_AFFECTED_DISABLED=1) as a belt-and-suspenders measure, per comment in `quality-gate.yml` L94–98. | Covered, one unverified edge |
| .snap files (vs inline) | SILENT-HOLE (edge case) | `.snap` extension isn't in `DEFAULT_RELEVANT_EXTENSIONS` (`changed-files.ts` L3–7) — filtered out before graph analysis entirely. Normally co-changes with its `.test.ts` (which IS tracked), so low practical risk. | 1 `.snap` file: `__tests__/unit/research/__snapshots__/prompts.test.ts.snap`. | Low blast radius |
| JSON imports | TRACKED | `.json` in `DEFAULT_RELEVANT_EXTENSIONS`; oxc-parser/oxc-resolver resolve `.json` static imports directly (`builder.ts` L26). | 1 file: `lib/utils/app-version.ts`. | Low usage |
| Virtual modules / resolve.alias from Vite plugins | PARTIAL | `builder.ts createResolver` (L19–36) only loads `tsconfig.json` — no `vite.resolve.alias` awareness. Runtime `importDurations` still record the real (aliased) resolved path once warm, but the static delta-parse path (`resolveFileImports`, L89: `if (result.error) continue`) silently drops any alias oxc-resolver can't independently resolve. | `vitest.config.mts` aliases `@revenuecat/purchases-capacitor` and `server-only` to local stubs (comment: package "may not be installed yet"); 5 files import these. Live risk on cache-miss / brand-new import. | Real, cache-miss-only |
| tsconfig path aliases | TRACKED | `builder.ts` explicitly loads `tsconfig.json` for oxc-resolver (L20–33). | `@/*` alias used pervasively (300+ hits via the vi.mock grep alone). | Covered, heavily exercised |
| Symlinked packages (agent-compounds) | N/A / untested | No `realpath()` call in `builder.ts`; symlink behavior under root-prefix check is unverified. | No agent-compounds symlink present in BCA's `node_modules`; the plugin itself is consumed via a sibling-dist path fallback in `vitest.config.mts`, not a symlink. | Not exercised |
| Type-only imports | TRACKED (correctly excluded) | `builder.ts` L52: `if (imp.entries.length > 0 && imp.entries.every(e => e.isType)) continue;` | N/A (correctness check, not a BCA-specific risk). | Covered |

## Part B — BCA replay-corpus bounds

1. **Adoption commit**: `7a89c10c` "chore: add vitest-affected plugin for intelligent test selection", 2026-02-23 (`git log -S "vitestAffected(" -- vitest.config.mts`). Commits on main since adoption: **2672** (`git log --oneline 7a89c10c..HEAD | wc -l`).
2. **Full-suite duration**: no direct wall-clock measurement found — CI (`quality-gate.yml` L92–98) runs `pnpm test:all` (sets `VITEST_AFFECTED_DISABLED=1`, always full suite) with no `timeout-minutes` recorded for that step. 619 test files (`find ... -name "*.test.ts*" | wc -l`), single-worker (`maxWorkers: 1`) — duration is an open/unmeasured constraint, not a citable number.
3. **Lockfile/config-touching commits**: **53 / 2672 (~2%)** touch `package-lock.json`, `pnpm-lock.yaml`, or `vitest.config.mts` (`git log --oneline 7a89c10c..HEAD -- <those 3 files> | wc -l`) — these force full-suite via `CONFIG_BASENAMES` and are useless replay material.
4. **Env requirements**: `vitest.config.mts` `test.environment: 'happy-dom'`, no `globalSetup`; default `include` excludes `__tests__/e2e/**` and `__tests__/supabase-integration/**` (both need live network/Supabase). `setupFiles` hardcodes `DEMO_EMAILS`. The default suite (619 files) needs no running Supabase/network at any historical commit — only the excluded integration/e2e suites do.
5. **Flakiness signals**: `retry: process.env.CI ? 1 : 0` (CI-only, masks nothing locally); `testTimeout: 15000` (raised from Vitest's 5000ms default — comment cites CPU starvation on a shared self-hosted runner, not Vitest concurrency); `maxWorkers: 1` for the same reason. All three are documented, real mitigations, not cosmetic.
6. **`.vitest-affected/` in BCA today**: exists, gitignored (`.gitignore` L238), contains `graph.json` (692KB) and `stats.jsonl` (41KB, live selection-decision log with `action`/`reason`/`durationMs` per run) — real evidence of live selection decisions to replay against.
