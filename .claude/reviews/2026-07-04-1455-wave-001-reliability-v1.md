# Review: wave/001 — reliability-v1 (Stage A frontier + Track 1)

**Date:** 2026-07-04 · **Branch:** wave/001 (6 bead commits vs main 7b82a76) · **Verdict:** APPROVED
**Reviewers:** security, performance, architecture (sonnet) + correctness (opus), round 1 · consensus via consensus.py

## Summary

| Category | Found | Auto-fixed | Beads filed | Rejected (rationale) |
|---|---|---|---|---|
| Security | 3 | 1 (memo cap, consensus) | 0 | 2 |
| Performance | 4 | 3 | 0 | 0 |
| Architecture | 4 | 2 | 1 (va-snl) | 1 |
| Correctness | 5 | 4 | 1 (va-um3) | 0 |
| **Total (deduped 16)** | **16** | **8** | **2** | **3** |

## Auto-fixed (commit on this branch)

1. **[Critical/perf]** `mergeRuntimeEdges` full-map copy per reporter flush → structural sharing (untouched Sets reused by reference; purity contract kept, documented). Identity test added. *(Conductor note: severity was inflated — a few hundred entries per run-end is sub-ms — but the fix is strictly better and keeps the A2-harness purity contract.)*
2. **[High/perf]** Cache-load realpath storm → `toConfinedReverseMap` trusts paths already under the canonical root (written canonically by save-side); alias/legacy entries still converge. Invariant documented.
3. **[High/arch]** `statsRootDir` now canonicalized at init (workspace/config-shape early-exit emissions).
4. **[High/arch]** Stats line taxonomy codified: DECISION lines (exactly one per configureVitest exit) vs DIAGNOSTIC lines (post-run, action `heartbeat`); heartbeat payloads slimmed (`strayCount`, no reused decision-field semantics); exact line-count assertions added; JSDoc + CHANGELOG updated.
5. **[High/correctness]** setupFiles + globalSetup triggers moved to the RAW changed set before relevance filtering (matching fullSuiteTriggers) — an `ignoreChangedFiles` rule matching a setup file can no longer cause silent under-selection. Two regression tests.
6. **[3-reviewer consensus]** `canonicalPathCache`: 10k cap with clear-and-continue, exported `clearCanonicalPathCache()` for tests, ancestor-walk routed through the memo, stale-symlink-mid-watch limitation documented.
7. **[Medium/correctness]** Selection self-verify made one-shot (selectedTests reset after verification) — kills watch-mode false alarms. Test added.
8. **[Medium/correctness]** Zero-edge heartbeat: stats line always, console.warn only when >5 modules ran (starvation signature) — small third-party-only selective runs stay quiet. Tests updated.

## Beads filed (Exhaust Rule)

- **va-snl** — Refactor: extract stats-emission + trigger helpers from the 516-line configureVitest (post-merge; same hot zone as A2*).
- **va-um3** — Investigate: `fullSuiteTriggers`/`ignoreChangedFiles` never match files between gitRoot and rootDir (semantic decision affecting consumer configs; feeds T2c).

## Rejected with rationale

- **`VITEST_AFFECTED_STATS_FILE` arbitrary write (security, Medium):** env-controlled output paths are standard local/CI dev-tooling behavior; the process already runs with the invoker's privileges — no privilege boundary is crossed.
- **`selectedFiles` absolute-path disclosure (security, Medium):** shadow mode is opt-in diagnostics; absolute paths in CI logs match vitest's own reporter output. Consumers who care scope stats files out of artifacts.
- **`scope: Set` speculative API (architecture, Medium):** not speculative — the scope parameter is the designed consumer contract for the replay harness's simulated selective cache evolution (bead va-yfn.3/A2b, plan §A2). Reviewer lacked plan context.

## Convergence round

Skipped deliberately (autonomous run): all Critical/High fixes carry fresh regression/identity/line-count tests, the conductor independently re-ran the full gate (tsc clean, build clean, 160/160), and personally inspected the two behavior-sensitive diffs (trigger relocation, cache prefix-trust). The exhaustive boundary check runs again at ac-merge.

## Validation

`npx tsc --noEmit` clean · `npm run build` clean · `VITEST_AFFECTED_DISABLED=1 npx vitest run` → **160/160** (155 post-wave baseline + 5 new fix tests).
